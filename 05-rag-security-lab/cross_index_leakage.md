# Cross Index Leakage in RAG

Cross-index leakage occurs when a retrieval system returns documents from unauthorized datasets.

Example:

HR user accessing security escalation workflow documentation

Impact:

Sensitive policy disclosure
Internal workflow exposure
Privilege boundary bypass
Unauthorized knowledge access

Mitigation:

Dataset-level access control
Metadata filtering enforcement
Query authorization validation
Retrieval boundary isolation
